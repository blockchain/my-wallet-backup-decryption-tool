{
  "name": "offline-decrypt",
  "version": "0.1.0",
  "description": "Decrypt wallets offline",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "postinstall": "node_modules/bower/bin/bower install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pack": "electron-packager ./ Blockchain --all --version=0.34.1 --icon=icons/icon.icns --overwrite --ignore=Blockchain-.*"
  },
  "author": "Justin Tormey",
  "dependencies": {
    "blockchain-wallet-client": "^3.1.3",
    "bower": "^1.6.5",
    "jquery": "^2.1.4",
    "node-env-file": "^0.1.7"
  }
}
